
import 'package:flutter/cupertino.dart';

class TempText extends StatelessWidget{
  int length;
  double size;


  TempText(this.length, this.size);

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}